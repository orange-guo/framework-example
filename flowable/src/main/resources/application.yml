server:
  port: 4396

spring:
  datasource:
    url: "jdbc:h2:mem:flowable;DB_CLOSE_DELAY=-1"
    username: "sa"
    password: ""
    driver-class-name: "org.h2.Driver"
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoint:
    flowable:
      enabled: true